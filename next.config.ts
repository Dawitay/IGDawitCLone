import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["cdn2.downdetector.com", "upload.wikimedia.org", "links.papareact.com"],
  /* config options here */
}
};

export default nextConfig;
